#pragma once

#include <Simfile/Common.h>

namespace AV {

/*

// Iterates through notes in a note set by row.
class NoteIterator
{
public:
	~NoteIterator();

	NoteIterator(const NoteSet& target);

	const Note* next(int& outColumn);

private:
	int myNumColumns;
	const Note** myCur;
	const Note** myEnd;
};

*/

} // namespace AV